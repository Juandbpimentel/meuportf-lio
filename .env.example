# Frontend environment variables for local setup
# Copy to .env.local and adjust values per environment

# Contact endpoint used by the frontend (POST). It can be an absolute URL or a relative path.
# Examples:
# VITE_CONTACT_ENDPOINT=http://localhost:3001/contact
# VITE_CONTACT_ENDPOINT=/contact
VITE_CONTACT_ENDPOINT=http://localhost:3001/contact

# Notes:
# - Never commit real secrets (API keys) into the repo.
# - Backend keys and provider secrets (SendGrid API key) must be kept on the backend only.
